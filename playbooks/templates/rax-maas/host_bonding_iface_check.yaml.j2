{% set label = "host_bonding_iface_status_check" %}
{% set check_name = label+'--'+inventory_hostname %}
type        : agent.plugin
label       : "{{ check_name }}"
period      : "{{ maas_check_period_override[label] | default(maas_check_period) }}"
timeout     : "{{ maas_check_timeout_override[label] | default(maas_check_timeout) }}"
disabled    : "{{ (check_name | match(maas_excluded_checks_regex)) | ternary('true', 'false') }}"
details     :
    file    : run_plugin_in_venv.sh
    args    : ["{{ maas_plugin_dir }}/bonding_iface_check.py"]
alarms      :
{% for interface in maas_bonding_interfaces %}
    host_bonding_iface_{{ interface }}_status :
        label                   : host_bonding_iface_{{ interface }}_status--{{ inventory_hostname }}
        notification_plan_id    : "{{ maas_notification_plan_override[label] | default(maas_notification_plan) }}"
        disabled                : {{ (('host_bonding_iface_{{ interface }}_status--'+inventory_hostname) | match(maas_excluded_alarms_regex)) | ternary('true', 'false') }}
        criteria                : |
            :set consecutiveCount={{ maas_alarm_local_consecutive_count }}
            if (metric["host_bonding_iface_{{ interface }}_status"] != 1) {
                return new AlarmStatus(CRITICAL, "The bonding interface {{ interface }} is down");
            }
{% endfor %}